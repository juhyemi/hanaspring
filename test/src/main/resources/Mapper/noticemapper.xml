<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hana.app.repository.NoticeRepository">
    <select id="select" resultType="NoticeDto">
        SELECT * FROM COMPANY_NOTICE ORDER BY NOTICE_DATE DESC
    </select>
    <select id="selectOne" parameterType="String" resultType="NoticeDto">
        SELECT * FROM COMPANY_NOTICE WHERE NOTICE_IDX = #{no}
    </select>
    <select id="selectResult" parameterType="String" resultType="NoticeDto">
        SELECT notice_idx, notice_title, notice_content, notice_member_id, DATE_FORMAT(notice_date,'%Y-%m-%d') as notice_date FROM COMPANY_NOTICE WHERE NOTICE_${category} like CONCAT('%', #{word}, '%')
    </select>
    <insert id="insert" parameterType="noticeDto">
        INSERT INTO COMPANY_NOTICE
        VALUES(0,#{noticeTitle},#{noticeContent},#{noticeMemberId},now())
    </insert>
    <select id="selectResultIntotal" parameterType="String" resultType="noticeDto">
        SELECT * FROM COMPANY_NOTICE
        WHERE LOWER(NOTICE_TITLE) LIKE CONCAT('%', #{keyword}, '%')
           OR LOWER(NOTICE_CONTENT) LIKE CONCAT('%', #{keyword}, '%')
           OR LOWER(NOTICE_MEMBER_ID) LIKE CONCAT('%', #{keyword}, '%')
    </select>
    <update id="update" parameterType="noticeDto">
        update company_notice set notice_content=#{noticeContent} where notice_idx=#{noticeIdx};
    </update>
    <select id="selectResultsort" parameterType="String" resultType="noticeDto">
        SELECT * FROM COMPANY_NOTICE
        ORDER BY NOTICE_${standard} ${sortorder}
    </select>
    <select id="selectResultcnt" parameterType="String" resultType="noticeDto">
        SELECT * FROM COMPANY_NOTICE
                          LIMIT ${cnt}
    </select>
</mapper>